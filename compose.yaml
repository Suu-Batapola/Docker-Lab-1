services:
  api:
    build: .
    ports: [ "5000:5000" ]
    environment:
      DATABASE_URL: postgres://app:app@db:5432/app
    depends_on: [ db ]
  db:
    image: postgres:16
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
